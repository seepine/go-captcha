# tmp_dir = ".tmp"

[build]
  delay = 150
  exclude_dir = ["assets", ".tmp", "vendor", "testdata", "docs", "web"]
  exclude_file = []

  include_dir = []
  include_ext = ["go", "tpl", "tmpl", "yml", "env"]
  include_file = []

  pre_cmd = [""]
